name: Deploy NodeJS app to AWS Beanstalk
on:
  push:
    branches:
      - "deploy-nodejs-beanstalk"

jobs:
  deploy:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3